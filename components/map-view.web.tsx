import React from "react";
import { View, Text } from "react-native";

// Web stub — NativeMap is only available on iOS/Android
export function NativeMap(_props: any) {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Map view is only available on native devices.</Text>
    </View>
  );
}
